//*This is how the grades should be allocated in the grade converter.*//
let grade;//The let is used to declare variables for the grades //
if (numeric > 79 ) { //The if else statements are used to show the conditions which should be executed once a code is run//
    grade = "A";
  } else if(numeric >= 60) {
    grade = "B";
  } else if(numeric >= 49) {
    grade =  "C";
  } else if(numeric >= 40) {
    grade = "D";
  } else if(numeric < 40) {
    grade = "E";
  }
